<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rangi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rangi">
        <activity
            android:name=".Rangi_Infomation"
            android:exported="false"
            android:label="@string/title_activity_rangi_infomation"
            android:theme="@style/Theme.Rangi.NoActionBar" />
        <activity
            android:name=".colourSelection"
            android:exported="false"
            android:label="@string/title_activity_colour_suggestion" />
        <activity
            android:name=".favouriteColours"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>